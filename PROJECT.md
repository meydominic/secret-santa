- Only as flutter web, nothing else
- responsive design for mobile and desktop
- Not interested in tests for now

## Ideas
- The user can add new names to the list
    - also names can be edited inline
- user can define (drop down with multi selection of already added names?) names which should not be matched
- there should be a button to "reroll" the matches
- each reroll should be saved in local storage (like a history) for up to 100 rerolls (if possible)
- should be possible to "load" a history entry